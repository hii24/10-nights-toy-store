# wally.toml — зависимости. Версии проверь на wally.run / в репозиториях.
[package]
name = "you/ten-nights-toy-store"
version = "0.1.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

[dependencies]
Knit = "sleitnick/knit@1.7.0"
Fusion = "elttob/fusion@0.3.0"
# ProfileStore (P1-08, персистентность) — ВЕНДОРНУТ как единый ModuleScript:
# vendor/ProfileStore.luau (официальный github.com/MadStudioRoblox/ProfileStore, by loleris).
# Сервер-онли → маппится в ServerScriptService.Server.Vendor (project.json), вне `src` (не линтится).
# Офиц. Wally-пакета в реестре нет; сторонним реаплоадам не доверяем.

[dev-dependencies]
TestEZ = "roblox/testez@0.4.1"
